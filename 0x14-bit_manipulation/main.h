#ifndef BITS
#define BITS
/* Standard Libraries */
#include <stddef.h>
#include <string.h>
/* Function Pointers */
unsigned int __pow(unsigned int a, unsigned int b);
unsigned int binary_to_uint(const char *b);
#endif /* BITS */
