#include "lists.h"
/**
 * list_len - gets total elements in a linked list
 * @h: pointer to first element
 * Return: # of elements printed
 */

size_t list_len(const list_t *h)
{
	int n;
	const list_t *tmp;

	readlinkedlist();

	return (n);
}
